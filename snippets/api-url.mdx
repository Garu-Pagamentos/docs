## URL Base

<CodeGroup>
```bash Produção
https://garu.com.br/api
```

```bash Sandbox
https://garu.com.br/api (use chaves sk_test_)
```
</CodeGroup>

<Note>
  A mesma URL é usada para teste e produção. A diferença está na chave de API: `sk_test_` para testes, `sk_live_` para produção.
</Note>
